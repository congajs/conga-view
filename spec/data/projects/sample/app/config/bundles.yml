# bundles to enable
bundles:

    all:

        # project bundles (from src/ directory)
        - demo-bundle
        - "@conga/framework-view-twig"

        # core conga bundles (from node_modules/ directory)
        #- conga-assets
        # - conga-bass
        #- conga-bower
        #- conga-client-template
        # - conga-rest
        # - conga-socketio
        # - conga-twig
        # - conga-validation

    test:

        # add bundles that are only used for tests
